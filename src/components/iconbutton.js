import React from "react";
import FeatherIcon from "feather-icons-react";

export const IconComponent = (props) => {
  return (
    <div>
      <FeatherIcon size="30"  {...props} />
    </div>
  );
};
